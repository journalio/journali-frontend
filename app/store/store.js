export default {
    state: {
        count: 0, // an example variable
    },
    mutations: {
        increment(state) {
            // An example mutator
            state.count++
        },
    },
}
